# API base URL
# For Vercel deployment (proxied via vercel.json rewrites):
VITE_API_BASE_URL=/api/v1
# For direct cross-origin (requires backend CORS config):
# VITE_API_BASE_URL=https://farm-management-api.vercel.app/api/v1
